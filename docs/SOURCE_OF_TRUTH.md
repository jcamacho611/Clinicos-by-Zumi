# KLINIKOS SOURCE OF TRUTH COMPATIBILITY POINTER

Version: `2026-08-27.2`
Status: `COMPATIBILITY POINTER - NOT A PARALLEL CANON`

Klinikos has one active product, architecture, business, experience, and product-design authority:

`docs/KLINIKOS_MASTER_CANON.md`

Machine-readable authority classification:

`docs/KLINIKOS_AUTHORITY_MAP.yaml`

Multi-agent execution control:

`docs/KLINIKOS_MULTI_AGENT_EXECUTION_CONTROL.md`

Current project/PR state snapshot:

`docs/KLINIKOS_CURRENT_PROJECT_STATE.md` and `docs/BRANCH_LEDGER.md`, when present and freshly verified.

## What defines current Klinikos

- **What Klinikos is / should become:** `docs/KLINIKOS_MASTER_CANON.md`.
- **What exists today:** current code, schema, migrations, tests, exact-head verification, verified deployment/runtime evidence, and verified external-connection evidence.
- **Who owns execution lanes:** `docs/KLINIKOS_MULTI_AGENT_EXECUTION_CONTROL.md`.
- **Why a decision exists:** subordinate evidence/provenance documents and Git history.
- **What Klinikos used to believe:** historical/superseded documents and older Git history.

This file intentionally contains no independent product model, lifecycle, wiring law, pricing model, specialist precedence chain, or competing architecture.

## Permanent conflict rule

Current verified implementation determines what exists today.

The Master Canon determines instituted product architecture and intended direction.

Specialist documents elaborate the Master Canon but cannot redefine Klinikos independently.

Execution bootstraps coordinate work but cannot override the Master Canon.

Historical documents cannot override current implementation truth or current product authority.

Accepted discoveries merge forward into the Master Canon instead of creating another competing snapshot.

## Current literal protected application order

Do not infer literal onboarding order from old value-first diagrams, old signup-first masters, old PR descriptions, or historical prototypes.

The current sequence is:

`PUBLIC DISCOVERY / SEARCH / REFERRAL / INVITATION`
→ `ENTER KLINIKOS`
→ `PROTECTED ACCESS TERMS + CONFIDENTIALITY / IP / RESTRICTED-USE AIRLOCK`
→ `SIGN IN OR CREATE ONE KLINIKOS IDENTITY`
→ `BIND AGREEMENT ACCEPTANCE TO IDENTITY / SESSION`
→ `RESTORE SAFE ENTRY CONTEXT`
→ `AUTHENTICATED ZUMI`
→ `INTENT / CONTEXT DISCOVERY`
→ `CLAIMS / RELATIONSHIPS AS NEEDED`
→ `PATH-AWARE VERIFICATION`
→ `ACTIVE EXPERIENCE ENVELOPE`
→ `EXPERIENCE ENGINE`
→ `PURPOSE-BUILT EXPERIENCE PROJECTION`
→ `REAL ACTION`
→ `FULFILLMENT / OUTCOME / EVIDENCE`
→ `MEMORY / NEXT ACTION`
→ `RETURN / CONTEXT SWITCH / EXPANSION`.

No permanent persona picker. No separate role accounts.

Public discovery may exist outside the protected app where deliberately approved, but the full personalized protected operating environment follows the sequence above.

## B + C hybrid

Klinikos uses purpose-built user/context experience families plus one continuously recomputed server-side Experience Engine. The experiences are projections, not separate products or authority systems.

## Prototype interpretation rule

Prototype statements are compressed architecture requirements, not merely copy.

Every material statement must be translated into the real identity, relationship, authority, workflow, evidence, financial, clinical, network, memory, and data infrastructure required to make it true.

## Final instruction

If another document contradicts this pointer, do not use this pointer as the deciding authority. Go directly to `docs/KLINIKOS_MASTER_CANON.md` and current verified implementation evidence.