# 04 — ZERO-CONTEXT AGENT HANDOFF

**Status:** SUBORDINATE EXECUTION PROTOCOL

## Read in this order

```
docs/KLINIKOS_MASTER_CANON.md
docs/grand-build/01_GRAND_BUILD_CONSTITUTION.md
docs/grand-build/01.5_RECONCILIATION_OVERRIDE.md
docs/grand-build/04_AGENT_HANDOFF.md   ← this file
current implementation
```

You have been given a task inside an existing company and repository.
**Do not assume the prompt describes current repository state.**

## First, establish truth

1. fetch latest `main` and record the exact SHA
2. read the Master Canon, Master Engineering Blueprint, Authority Map
3. inspect recent commits and open PRs
4. identify overlapping branches
5. inspect relevant schema, services, components and tests
6. inspect CI and current security evidence
7. inspect production/runtime evidence when relevant
8. inspect branch protection and repository visibility
9. query live Stripe when commercial truth matters

## Then produce

```
/current       what actually exists, with paths and SHA
/target        what the Canon requires
/gap           what remains
/conflicts     anything contradictory or stale
/reuse         existing components/services/models to extend
/risk          security, clinical, commercial, UX, migration
/design        proposed solution
/implementation ordered work
/tests         proof required
/companyvalue  why this matters economically or strategically
```

## Do not

Create duplicate systems · erase accepted functionality · merge intentionally RED PRs ·
claim targets are actual · claim integrations are live without proof · change pricing from
stale history · make organizations free · make Person entry paid · expose proprietary server
logic · use UI as authorization · make AI authority · make 3D required for essential
workflows · rewrite functioning systems merely because rewriting is easier.

## Words that are not interchangeable

```
DESIGNED · PLANNED · CODED · TESTED · CI GREEN · MERGED · DEPLOYED · PRODUCTION VERIFIED
```

If you have only proved `CODED`, do not call it production-ready.

## Exact-head verification

```bash
npm run governance:traceability
npm run security:check
npm run db:validate
npm run db:generate
npm run type-check
npm run lint
npm test
npm run test:mvp
npm run build
git diff --check
```

Plus, where applicable, the GitHub jobs `Quality / verify` and `Quality / deploy-contract`
**at the exact head SHA**, browser workflows, mobile 390px, reduced motion, 200% zoom,
WebGL2-disabled Precision Mode, and confidentiality gates.

Record run IDs and timestamps. A workflow object existing is not evidence of green.
Mergeable-state is not CI-green.

## Branch and merge discipline

Work on an isolated branch. **Never work directly on `main`. Never merge without explicit
founder approval.** RED before GREEN. Small, independently reviewable tranches. Never weaken
a failing test to get green.

## Scope discipline

Do not mix a repo-wide commercial migration into a renderer PR. Do not mix a security gate
into a copy change. **Scope mixing is a recognised failure pattern here** — one PR, one
coherent outcome.

## When a tool is blocked

A blocked tool is a routing problem, not a project blocker:

```
PRIMARY PATH → FALLBACK A → FALLBACK B → NATIVE IMPLEMENTATION → MANUAL-BUT-TRUTHFUL PATH
```

Never stop at "this requires premium." Ask what other available capability produces the same
business outcome. Prefer `REUSE → OPEN STANDARD → EXISTING PROVIDER → ALTERNATE PROVIDER →
INTERNAL IMPLEMENTATION`.

**But do not route around a blocker** if doing so would spend company funds, enter a
contract, send external communications, accept a meeting, make a legal attestation, weaken a
security or privacy control, expose PHI/PII, materially alter approved architecture, change
commercial terms already offered, create regulated authority, or fabricate evidence. Those
need the appropriate authority.

Capability use is **applicability-based, never ceremonial.** Do not invoke a connector to
look thorough. Invoke it when its actual system is in scope.

## Final company law

```
ONE KLINIKOS · ONE PERSON · ONE GRAPH · ONE ZUMI · ONE AUTHORITY MODEL
ONE CONTINUOUS HEALTHCARE REALITY
```

Backend complexity creates frontend simplicity. Do not make the person learn the software.
Make the software understand the person's situation.

---
_Generated by [Claude Code](https://claude.ai/code)_
