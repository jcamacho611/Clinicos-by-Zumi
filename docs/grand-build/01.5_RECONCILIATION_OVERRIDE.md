# 01.5 — RECONCILIATION OVERRIDE

**Status:** SUBORDINATE ACTIVE OVERRIDE — NOT A CANON. Subordinate to `docs/KLINIKOS_MASTER_CANON.md`.

```
DOCUMENT_VERSION:        2026-09-05.2
STATUS:                  ACTIVE_OVERRIDE
LAST_VERIFIED_MAIN_SHA:  dd385aba249a7c6dd53f2baedb98b48ca6b3bb65
LAST_VERIFIED_AT:        2026-09-05
REVERIFY_IF_MAIN_MOVES:  YES
```

**Read after `01`, before `02`.**

Precedence: if this conflicts with `02`, **this wins**. If this conflicts with
`docs/KLINIKOS_MASTER_CANON.md`, **the Canon wins**. If any claim here about *current
implementation* conflicts with current code or runtime, **current evidence wins**.

This document ages faster than the Constitution. Re-verify it whenever `main` moves.

## /0 — Baseline

Do not treat the Living Reality as greenfield. P01 + P16 merged. Before anything:

```bash
git fetch origin && git rev-parse origin/main && git log --oneline -20 origin/main
```

Canonical paths that already exist — **reuse these**:

```
src/lib/living-reality/          reality-projection, runtime-mode, member + public adapters
src/components/living-reality/   canvas, layer, scene
```

Order remains: **REUSE → EXTEND → GENERALIZE → CONNECT → PARTNER → BUILD NEW.**

## /1 — Directory law

`02` may reference `src/lib/reality/` and `src/components/reality/`. **Do not create those.**
They would be a second runtime under a different name. Use `living-reality/`.

Never create: `RealityV2`, `LivingRealityV2`, `UniverseV2`, `Grid2`, `Zumi2`, another theme
layer, another `RealityProjection`, another performance governor, another canvas authority.

## /2 — Dependency law

Preserve the current family. **Do not downgrade.**

```
react 19.x · three 0.185.1 · @react-three/fiber 9.7.0 · @types/three 0.185.x
```

R3F 9 is the React 19 line. **R3F 8.x requires React 18 and would break this repo.** Any
instruction to install `@react-three/fiber@^8` or `three@^0.169` is stale — ignore it.

No Drei, no postprocessing, no physics in the first runtime tranche. To add Drei later,
present `/reason /bundle-impact /manual-alternative /performance /security /maintenance`
and get architectural approval first.

## /3 — WebGL2

Current Three.js uses WebGL2. Detection must distinguish: WebGL2 available · unavailable ·
context failed · context lost. If unavailable → `PRECISION_MODE`, fully functional. Do not
build a WebGL1 fallback.

## /4 — Performance vs motion are separate concerns

Keep three **performance** modes: `FULL_REALITY` · `BALANCED_REALITY` · `PRECISION_MODE`.
Do not add `STATIC` as a fourth performance mode. Motion is its own axis:

```ts
type RealityMotionPolicy = "FULL_MOTION" | "REDUCED_MOTION" | "STATIC_COMPOSITION"
```

`prefers-reduced-motion` must never produce a blank scene.

## /5 — Idle means idle

`frameloop="demand"` is canonical. Do not write `state.invalidate = false`. Stop calling
`invalidate()` once bounded animations settle. Target **0 idle draw calls**.

Allowed: arrival, camera transition, active-object transition, brief attention movement,
short Zumi transition, theme transition, user-driven exploration.
Forbidden as permanent loops: endlessly rotating rose, perpetually crawling edges,
decorative particles, continuous camera drift.

An unresolved edge is dashed and louder **without** crawling forever.

## /6 — One token authority

`src/app/design-tokens.css` is the semantic authority. Shell tokens and Reality tokens
**derive** from it. Do not create a disconnected parallel palette. Legitimate additions look
like `--k-reality-fog-near`; illegitimate ones look like `--new-obsidian-black`.

## /7 — Typography

Do not silently replace Klinikos typography as part of a renderer build. Bodoni Moda /
Archivo / JetBrains Mono are a **design option**, not current product truth. Any change
needs prototype, comparison, bundle measurement, accessibility review and design approval.

## /8 — Reality registry

Approved in principle, and it must extend existing infrastructure. Derive the union from the
registry so they cannot drift:

```ts
export const REALITY_REGISTRY = { PUBLIC: {...}, HOME: {...}, GRID: {...} } as const
export type RealityId = keyof typeof REALITY_REGISTRY
```

Each Reality declares: canonical plane · route family · minimum disclosure · default camera
intent · precision workspace behavior · commercial gate if any · FUR definition.

## /9 — Evolve the contract

Do not delete and replace the merged `RealityProjection`. Add capability through explicit
fields plus tests. Never spread ORM or domain objects into a projection.

## /10 — Never ship raw attention weights

`02` proposes `gravity: number` while also forbidding proprietary weights in the browser.
Those conflict. **Server computes attention; the client receives a level and a reason.**

```ts
type AttentionLevel = "normal" | "elevated" | "critical"
{ nodeId, level, explanation }   // explanation is plain language
```

Never expose raw score, ranking weights, feature contributions, anti-gaming thresholds,
the attention formula, or commercial weighting.

## /11 — No PHI in the spatial projection

Even on clinical routes. Current Visit may show authorized PHI in the **precision DOM**.
The scene never needs patient name, MRN, DOB, diagnosis, chief complaint, medications,
allergies, note text, ICD, CPT, results, or payer/member identifiers.

PHI-free structural signals are fine where the label itself is safe: "Current Visit",
"3 items need review", "Documentation incomplete". Test scene projections independently
from authorized precision DOM.

## /12 — No omnipotent projection endpoint

Do not accept `{reality, objectId, intent}` and query arbitrary domains. Route:

```
REQUEST → SESSION → EXPERIENCE ENVELOPE → AUTHORITY CONTEXT → REALITY REGISTRY
→ APPROVED REALITY BUILDER → ALREADY-AUTHORIZED DOMAIN SERVICE
→ MINIMUM NECESSARY PROJECTION → REDACTION → RESPONSE
```

The registry chooses the legal builder; the client never chooses arbitrary domain access.
Server components may keep building projections directly. Add `/api/reality/projection` only
where interactive client refresh genuinely requires it, with origin/CSRF validation, and
`private, no-store` on authenticated responses.

## /13 — One canvas is not one disclosure context

Never preserve sensitive projection state across logout, login, Person switch, organization
switch, role-context switch, or a public↔authenticated boundary. On any authority or
disclosure change: clear projection, renderer state, focus, cached labels; invalidate
buffers; re-resolve server context.

**Security beats continuity.**

## /14 — Accessibility twin

The existing semantic DOM **is** the precision and accessibility authority. Do not mount a
visible interactive ActionDock and a second hidden interactive one. Use the same semantic
control layer: overlaying the canvas in cinematic mode, primary in Precision Mode. The
canvas stays `aria-hidden` unless a genuinely accessible spatial interaction is designed.
No consequential task may exist only in WebGL.

## /15 — Zumi does not patch truth

Never apply a model-returned `Partial<RealityProjection>` directly. Route:

```
USER INTENT → ZUMI INTERPRETATION → PRESENTATION PROPOSAL → SERVER VALIDATION
→ AUTHORIZED RETRIEVAL → DETERMINISTIC COMPOSER → REALITY PROJECTION
```

The model may propose `{line, requestedReality?, requestedFocusIds?, requestedLens?,
suggestedQueries?, draftActions?}`. The **server** decides whether a requested object exists
and may be shown. Zumi never sets authority, verification, truth class, eligibility,
payment, evidence, clinical status, or enables a consequential action.

## /16 — Demand Escrow must be truthful

Public visitor, no verified supply:

> No verified opening matches this yet. Klinikos can watch for one.

Do **not** say "I've saved this" before persistence exists. Pre-signup the request is
ephemeral, session-bound, non-authoritative. After free Person signup the Need is persisted,
the watch is active, notification preference is explicit — then and only then:

> Watching for RN opportunities around Queens.

## /17 — Demand intelligence privacy

Aggregate demand statements require an explicit aggregation policy: minimum cohort
threshold, coarse geography, time bucketing, sparse-cell suppression, no PHI, no sensitive
targeting, no individual reconstruction.

Never call someone a verified RN because they searched for RN jobs. Distinguish
*people interested in RN roles* / *claimed RNs* / *verified RN professionals*, and let
aggregate statements inherit that distinction.

## /18 — FUR with privacy

Every Reality defines what counts as a useful result. Emit
`reality.fur.reached {actorAnalyticsKey, realityId, furKind, msFromArrival, truthClass,
experimentId?, occurredAt}`. No raw Person IDs where avoidable, no PHI, no clinical content.
A click is not automatically a useful result.

## /19 — Attach-rate numbers are scenarios

Do not reproduce attach-rate arithmetic in investor, lender, product or engineering output
unless the supporting model states member base, attach definition, conversion model, ARPA,
plan mix, retention and period. FUR remains strategically important; the ARR outcome remains
`SCENARIO`.

## /20 — Authority Passport

Answer progressively: who is this person · what do they claim · what evidence supports it ·
what source verified it · where · for what role · for which organization · under which
policy · when does it expire · what is missing · who makes the final decision.

```
CLAIM ≠ VERIFICATION · VERIFICATION ≠ PRIVILEGE · LICENSE ≠ AUTHORITY FOR EVERY TASK
MATCH ≠ ELIGIBILITY · PAYMENT ≠ AUTHORITY
```

It is a governed evidence and decision-support layer, not a universal license. Its economic
value is a **strategic thesis** until evidence proves it.

## /21 — Scene animation

Camera presets in `02` are design starting values. Put them in one configuration module; do
not duplicate across components. Visual regression may tune position, lookAt, FOV and
duration without changing business semantics.

## /22 — Precision workspace performance

When precision work opens: pause invalidations → reduce visual intensity → lower opacity →
cheap compositing → bring DOM forward. Use blur only if measured safe. Clinical, charting,
claim and contract work prioritize clarity, responsiveness, battery and accuracy over
spectacle.

## /23 — Bundle budgets start from a measured baseline

Do not fail CI on arbitrary thresholds before measuring. Record initial route JS, async
Living Reality chunk, CSS, font payload and 3D contribution first, then ratchet. The
180 KB / 320 KB figures in `02` are **targets until measured**.

## /24 — Cold start

Treat the reported ~25s cold start as important but **dated**. Create a current performance
evidence task: warm TTFB, cold TTFB, FCP/LCP, host state, timestamp, deploy SHA. If a
sleeping host is still costing tens of seconds on first paint, it is a P0 growth defect.

## /25 — P16 remains binding

Until evidence changes it: **PRODUCTION PHI = BLOCKED.** Spatial success does not imply
HIPAA compliance, clinical authority, payment authority or vendor readiness. Treat branch
protection and repository visibility per live evidence, not per memory.

## /26 — R1–R7 are evolution tranches

R1 contract *evolution* · R2 token/material *convergence* · R3 semantic/precision
*convergence* · R4 runtime *hardening* · R5 behavior primitives · R6 public value + Demand
Escrow + FUR · R7 Grid + Current Visit projection. Nothing here says "introduce the first
Canvas."

## /27 — The next behavior layer

The highest-value missing capability is not more geometry:

1. Active Object vs Focused Object · 2. deterministic camera grammar · 3. semantic
relationship taxonomy · 4. attention gravity · 5. PAST ← NOW → FUTURE · 6. Zumi visible
state · 7. Mission Rooms · 8. precision workspace transition · 9. selection → inspector →
governed action · 10. outcome → world recomposition · 11. public ecosystem convergence ·
12. Grid spatial convergence.

## /28 — Play-by-play remains mandatory

Never write "the user opens a dashboard." Use the frame contract in `02`.

## /29 — Starting procedure

Read Canon → `01` → `01.5` → `02`. Run discovery. Report `/current /reuse /gap /collisions
/lane /security /performance /commercial /risk`, explicitly naming current `main` SHA, the
P01/P16 implementation, Three/R3F versions, existing Living Reality directories, the
existing `RealityProjection`, the token authority, open PR collisions, current P16 blockers
and current branch-protection evidence. **Then stop** until the lane is approved.

## /30 — Final law

```
ONE KLINIKOS · ONE LIVING REALITY SPINE · ONE SERVER-OWNED PROJECTION CONTRACT
ONE SEMANTIC DOM AUTHORITY · ONE SPATIAL RUNTIME · ONE TOKEN AUTHORITY
ONE ZUMI · ONE EVIDENCE/AUTHORITY SYSTEM
```

Do not build a prettier dashboard. Do not build a 3D screensaver. Do not rebuild what
already exists. Build the healthcare operating reality.

---
_Generated by [Claude Code](https://claude.ai/code)_
